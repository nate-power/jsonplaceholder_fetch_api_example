import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jsonplaceholder_fetch_api_example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
